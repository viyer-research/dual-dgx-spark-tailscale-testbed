> This README is a placeholder committed via the GitHub web UI to create the main branch. Full content (including tables and links) was uploaded from the local repo immediately after in the following commits — see docs/, scripts/, and troubleshooting/ for the complete runbook.
>
> Dual DGX Spark + Tailscale: A Remote-Access AI Testbed. Deployment runbook for a two-node NVIDIA DGX Spark cluster used for distributed LLM training, made remotely accessible over a Tailscale mesh VPN.
> 
